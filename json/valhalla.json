{
  "name": "Valhalla",
  "slug": "valhalla",
  "categories": [
    0
  ],
  "date_created": "2026-09-10",
  "type": "ct",
  "updateable": true,
  "privileged": false,
  "architectures": ["amd64"],
  "interface_port": 8002,
  "documentation": "https://valhalla.github.io/valhalla/",
  "website": "https://valhalla.github.io/valhalla/",
  "logo": "https://cdn.jsdelivr.net/gh/selfhst/icons@main/webp/valhalla.webp",
  "description": "Valhalla is an open-source routing engine and accompanying libraries for use with OpenStreetMap data. It provides turn-by-turn routing, map matching (snapping noisy GPS traces to the road/path network), time+distance matrices, isochrones and elevation lookups over a self-built tile graph. Commonly run as a self-hosted routing backend so that location data never has to be sent to a third party.",
  "install_methods": [
    {
      "type": "default",
      "script": "ct/valhalla.sh",
      "config_path": "/opt/valhalla_data/valhalla.json",
      "resources": {
        "cpu": 4,
        "ram": 8192,
        "hdd": 24,
        "os": "Debian",
        "version": "13"
      }
    }
  ],
  "default_credentials": {
    "username": null,
    "password": null
  },
  "notes": [
    {
      "text": "Installed by compiling from source (no prebuilt package exists), which takes roughly 20-45+ minutes depending on CPU/RAM.",
      "type": "info"
    },
    {
      "text": "No routing tiles are built during install. Find your region's extract at https://download.geofabrik.de/ (browse to a continent, then country/region, and copy the .osm.pbf download link), then build the graph with: /opt/valhalla_data/build-tiles.sh <pbf-url>. Avoid planet-wide data unless the container is sized for it.",
      "type": "warning"
    },
    {
      "text": "The HTTP API on port 8002 has no authentication - keep it on a private/internal network.",
      "type": "warning"
    },
    {
      "text": "To use this as the map-matching backend for GeoPulse, set GEOPULSE_TIMELINE_MAP_MATCHING_ENABLED=true, GEOPULSE_TIMELINE_MAP_MATCHING_PROVIDER=valhalla and GEOPULSE_TIMELINE_MAP_MATCHING_VALHALLA_BASE_URL=http://<this-container-IP>:8002 on the GeoPulse backend.",
      "type": "info"
    },
    {
      "text": "The default 24GB disk / 8192MB RAM only covers the build. Routing tiles need several times the compressed extract size in free disk, so resize the container before building tiles for large regions.",
      "type": "warning"
    }
  ]
}
